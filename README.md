# NYC Urban Data Analytics

## How to start the project

### Start development database

```bash
docker compose --project-directory "devdb-nyc" -f dev.docker-compose.yml up -d
```

### Start as production

```bash
docker compose up -d
```
